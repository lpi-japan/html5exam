# Web プロフェッショナル認定 改訂企画書

本書は、HTML5プロフェッショナル認定の改訂が目指す方向性と差別化ポイントを概説する。

---

## 1. 現行試験の改善点と改訂の方針

現行試験（ver.2.5）は Level 1（HTML/CSS）と Level 2（JavaScript・Web API）という 1 系統 2 レベルの構成で、Web 標準の知識を網羅的にカバーすることを軸に設計されてきた。以下の 3 点について、現状の問題と改訂の方針を示す。

### 1. 試験構成：学習の順序制約をなくす

Level 1 と Level 2 は「難易度の違い」ではなく「領域の違い」に近い状態になっており、プログラミング系の受験者がマークアップ領域の Level 1 を経由しなければ Level 2 に進めない、という学習の順序制約が生じていた。

領域を **マークアップ** と **プログラミング** の 2 系統に分割し、それぞれに Level 1 / Level 2 を設ける構成とする（現時点の想定は計 4 試験）。受験者は専門領域から学習を開始できる。マークアップ Level 1 は旧 Level 1 の HTML/CSS を再編し、マークアップ Level 2 は高度な CSS 設計とアクセシビリティを中心にほぼ新設する。プログラミング Level 1 は JavaScript 基礎と DOM 操作を扱う新設の試験であり、プログラミング Level 2 は旧 Level 2 の JavaScript・Web API を再編・拡張して設計判断を厚くする。

Level 1 の 2 試験分割は比較的固まりつつある。**上位レベル（Level 2）を何試験・何認定にするかは未確定**であり、§3 の割当表では整理の便宜上、試験横断で「レベル2」列にまとめている。

### 2. 上位試験の再定義：「判断の試験」として

上位レベル（Level 2）は、個別の仕様の記憶ではなく、**技術選定・設計の判断根拠**を問う水準とする。与えられた状況でどの技術を選び、その理由を説明できることを評価する。状況提示型の問題を通じて応用・分析レベルの判断力を中心に置く。判断の対象はキーワードごとに分かれるが、**クライアントとサーバを通した Web システム全体**として読む力が Level 2 の一行概要に置かれる（§3）。

現場では、見た目が意図とずれる理由（カスケード・詳細度）、通信が失敗する理由（CORS・認証の前提）、変更が他の層へ波及する経路といった論点で詰まりやすい。本改訂は難易度の段階付けより、**どの領域を試験に含め、どの水準まで問うか**の設計が主題である。Level 2 では、SPA/MPA や認証フローの選択のように、複数の要素技術を比較して説明できるかを問う。

### 3. AI 時代への対応：影響範囲の感覚を測る

生成 AI の普及により仕様の暗記だけでは差別化しにくくなった。AI に任せると見落とされやすいセキュリティ（CSP・認証フロー・CORS）やパフォーマンス（レンダリングパス・リフロー）の副作用が典型である。本試験が測るのは、**ある実装が別の仕様・パフォーマンス・セキュリティへどう波及するかを読む力**である。

**測らないもの：** 特定フレームワーク・ライブラリの操作手順、仕様の暗記。フレームワークの選定根拠は問うが、フレームワーク固有の API は問わない。

---

## 2. 試験の基本設定（改訂案）

現行試験は 2 試験構成だったが、改訂後は最大 4 試験想定（Level 2 の分割は §3 参照）。各試験の個別設定は検討中。

| 項目 | 内容 | 根拠 |
|---|---|---|
| 試験数 | 4 試験想定（マークアップ L1/L2・プログラミング L1/L2）。Level 2 の分割は未確定 | 領域の独立性を担保し、受験者が専門領域から入れるようにするため |
| 問題形式 | 多肢選択式（状況提示型を上位レベルに多く配置） | 設計判断・技術選定を問うには状況を設定した問題形式が必要 |
| 評価の重心 | 上位レベルは判断・選択を問う問題の比率を高める | 知識確認型だけでは応用・分析レベルの評価が困難なため |
| 評価方法 | 未定（現行と同様の合否方式が基本。検討中） | — |

**参考: 現行試験（ver.2.5）**

| 項目 | 現行 |
|---|---|
| 試験数 | 2 試験（Level 1・Level 2） |
| 問題数 | Level 1: 60問、Level 2: 60問 |
| 試験時間 | Level 1: 90分、Level 2: 90分 |
| 評価方法 | 閾値による合否方式 |

---

## 3. 各試験の概要と技術領域の割り当てイメージ

各試験の「確認できるスキルレベル」で用いる一行概要と、観点語（キーワード）の試験間割当のたたき台である。出題範囲本文・スキルレベルの bullet は本節を基準に差分調整する。

**レベル2列について：** マークアップ・プログラミングの上位水準をひとまとめにしている。本表のレベル2列は試験分割案ではなく能力領域の地図であり、**何試験・何認定にするかは未確定**である。

**記号：** ◎＝その列のトップ行（スキル bullet）候補　○＝トップ行以外で触れる　△＝薄い・境界のみ　—＝トップキーワードにしない

**行にしない語（要約）：** 「設計・選択」「バックエンド」は Level 2 の一行概要に含める。可用性（インフラ寄り）・保守（再利用・モジュールへ分散）も独立キーワードにしない。

### 一行概要

各試験ファイルの「確認できるスキルレベル」直下に置く一文。下表の ◎ 行の要約である。

**マークアップ Level 1** — 通信・文書・スタイル・表示環境の各指定が及ぼす影響を読み、選択や判断ができる。

**プログラミング Level 1**（改訂案）— プログラムの構造とデータを理解し、モジュール構成・インタラクション・通信とセキュリティの前提を踏まえて判断できる。

※ プログラミング Level 1 の現行文案は `objectives_programming.md` に残っている。6 行化と本一行概要への反映はこれから。

**レベル2**（試験横断・仮）— クライアントとサーバを通した Web システムについて、通信・状態・検証・品質の各観点で要素技術を比較し、全体としての設計判断ができる。

### キーワード割当

| キーワード | マークアップ L1 | プログラミング L1 | レベル2 |
|---|---|---|---|
| **通信** | ◎ リクエスト・レスポンス、フォーム送信（`method` / `action` / `enctype`）、HTTPS — 文書・フォームから見える HTTP | ◎ **非同期処理と通信**のうち fetch、ステータス・ボディ・ネットワークエラー — スクリプト起点の手順 | ○ WebSocket・SSE、fetch の詳細制御、キャッシュ戦略。サーバの API 境界・責務分担 |
| **セマンティクス** | ◎ 要素・属性の意味、見出し階層、ランドマーク — マークアップ選択 | — | ○ 構造化・意味づけの設計判断。**アクセシビリティ**と接続 |
| **プレゼンテーション** | ◎ セレクタ・カスケード・詳細度、見た目の追跡 | — | ○ 複雑レイアウト・アニメーション・トランジションの制作 |
| **インタラクション** | ◎ フォーム、擬似クラス、トランジション — HTML/CSS による宣言的操作 | ◎ DOM・イベント・フォーム制御 — スクリプトによる動的応答 | ○ DOM 応用（Observer 等）、History API、Web コンポーネント。**状態管理**の UI 側と接続 |
| **環境差** | ◎ viewport、メディアクエリ、レスポンシブ | △ ウィンドウ寸法・スクロール等 — トップ行にはしない | ○ 表示・リソースの環境適応。**パフォーマンス**と接続 |
| **プログラムの基本構造** | — | ◎ 文法・型・制御・オブジェクト・クラス骨格 — 実行結果の読解 | ○ 高度な関数操作、Proxy、generator 等 |
| **コレクションとデータ** | — | ◎ 配列の走査・変換・集約（`map` / `filter` / `reduce` 等）、JSON、文字列・RegExp | ○ Map/Set、高度なコレクション操作。**データモデル**と接続 |
| **モジュールと配布** | — | ◎ ES Modules、依存・`package.json`、バンドルとブラウザへの届け方 — 構成・配布の読解 | ○ npm 構築、lock 運用、ポリフィル、Babel/TypeScript、ツールチェーンの設計 |
| **非同期処理と通信** | — | ◎ Promise・async/await、タイマー、fetch（**通信**と一体のトップ行） | ○ `Promise.all` 等、並行パターン、非同期ジェネレータ |
| **セキュリティ** | ○ HTTPS・混在コンテンツ — **通信**のサブ | ◎ セキュアコンテキスト、オリジン/CORS、API 権限、依存の取り込み | ◎ CORS/SSL モデルの設計、認可境界、サプライチェーンリスク、手法選択 |
| **デバッグ** | — トップ行にしない。開発者ツールによる要素・スタイル・ネットワークの確認 | — トップ行にしない。コンソール・ブレークポイント・実行時間の計測 | ○ 障害切り分け、**パフォーマンス**計測との併用 |
| **状態管理** | ○ フォームの宣言的な状態 | ○ DOM 上の UI 状態、非同期完了後の画面更新 — **インタラクション**のサブ | ◎ クライアント・サーバ・キャッシュの一貫性、同期タイミング、競合・二重送信 |
| **バリデーション** | ○ HTML 制約属性・宣言的検証 | ○ 制約検証 API、送信前チェック — **インタラクション**のサブ | ◎ 層ごとの検証分担、信頼境界、API エラーと UI 表示の整合 |
| **データモデル** | ○ 表・リストのマークアップ構造 | ○ JSON・オブジェクト形状の読解 — **コレクションとデータ**のサブ | ◎ API 契約・スキーマ、フロント表示とサーバデータの対応（独立行とするか要検討） |
| **認証・認可** | ○ ログインフォーム等 | ○ トークン付きリクエストの読解 — 概要 | ◎ セッション・トークン・Cookie、フロントとサーバの責務分担（**セキュリティ**への統合要検討） |
| **アクセシビリティ** | ○ 代替テキスト等 — **セマンティクス**のサブ | — | ◎ 要件に応じた a11y 手法の選択、キーボード・ARIA の設計判断 |
| **パフォーマンス** | ○ 画像・読み込み等の作者側配慮 | ○ 実行時間計測 — **デバッグ**と接続 | ◎ 読み込み・描画・ネットワークの最適化。オフライン時 UX は **機能領域 API** と接続 |
| **再利用・構成** | — | — | ◎ HTML/CSS の設計手法、Web コンポーネント、Shadow DOM — 部品化と保守性 |
| **機能領域 API** | — | — | ◎ 用途別 Web API の選択・実装。メディア、ストレージ、リアルタイム、デバイス — 汎用 API は L1 の既存行へ |

**トップ行の並び（案）** — 上表の ◎ 行。

- **マークアップ L1**（5 行）：通信 → セマンティクス → プレゼンテーション → インタラクション → 環境差
- **プログラミング L1**（6 行・改訂案）：基本構造 → コレクションとデータ → モジュールと配布 → インタラクション → 非同期処理と通信 → セキュリティ
- **レベル2**（仮）：状態管理 → バリデーション → データモデル → 認証・認可 → セキュリティ → アクセシビリティ → パフォーマンス → 再利用・構成 → 機能領域 API（＋各 L1 キーワードの L2 深化）

※ **セキュリティ**はプログラミング L1 のトップ行にも載る（L1/L2 両方で ◎）。**モジュールと配布**は L1 で ◎、L2 では ○。

---

## 4. 現状の出題範囲の構成と各主題の設計意図

出題範囲の詳細は `objectives_markup.md` / `objectives_programming.md` に掲載する。各主題のスコープ判断の根拠は `decision_records.md` に記録する。

設計の基本原則は、「この技術を知らないと、実務でどんな判断ができないか」を起点に範囲を決めることである。仕様の網羅より、判断の根拠になるかどうかを優先する。各試験ファイル冒頭の「確認できるスキルレベル」はこの原則に沿った能力概要であり、キーワードごとの割当は §3 に置く。

---

## 5. 現時点の出題範囲検討における主な課題・懸念点

- 上位レベル（Level 2）の試験・認定の分割と、§3 レベル2列の対応付け
- §3 と各試験ファイルのスキルレベル（一行概要・bullet）・出題範囲の同期
- プログラミング Level 1 の 6 行化、ドメイン特化 API の Level 1 からの移動
- 不足・過剰な観点がないかの洗い出し
- AI に任せても問題ない領域と人間が理解していなければならない領域の境界
- マークアップ / プログラミングの分割が、現場の職種区分と合っているか
