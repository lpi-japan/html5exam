# Web プロフェッショナル認定 改訂企画書

本書は、HTML5 プロフェッショナル認定の改訂方針をまとめたものである。読者は改訂作業の関係者を想定し、何を変えるか（第 1 節）と各試験で何を確認するか（第 2 節の一覧）を中心に読む。

---

## 1. 現行試験の改善点と改訂の方針

現行試験（ver.2.5）は、Level 1 が HTML/CSS、Level 2 が JavaScript・Web API という 1 系統 2 レベルの構成であった。Web 標準の網羅を重視した設計のため、次の 3 点を改める。

### 1. 試験構成：学習の順序制約をなくす

問題： Level 1 と Level 2 は難易度差というより領域差に近いのに、プログラミングを学ぶ受験者もマークアップ Level 1 を経由しないと Level 2 に進めなかった。

方針： 領域を マークアップ と プログラミング の 2 系統に分け、それぞれに Level 1 / Level 2 を設ける（現時点の想定は計 4 試験）。受験者は自分の領域から始められる。

旧試験との対応は次のとおり。マークアップ Level 1 は旧 Level 1 の HTML/CSS を再編する。マークアップ Level 2 は高度な CSS とアクセシビリティを中心にほぼ新設する。プログラミング Level 1 は JavaScript 基礎と DOM 操作の新設試験である。プログラミング Level 2 は旧 Level 2 の JavaScript・Web API を再編し、設計判断を厚くする。

Level 1 の 2 試験への分け方はおおむね固まった。Level 2 を何試験・何認定にするかは未確定である。第 2 節の一覧では、試験分割が決まるまでの作業用に、上位水準を「レベル2」と一列にまとめている。

### 2. 上位レベル：設計の判断を問う

方針： Level 2 は仕様の暗記ではなく、状況に応じた技術選定とその理由を問う。多肢選択式のまま、状況提示型の問題を Level 2 で増やす。

改訂の主題は難易度の段階付けではなく、どの技術領域を試験に含め、どこまで問うかである。実務では、見た目がずれる理由（カスケード・詳細度）、通信が失敗する理由（CORS・認証の前提）、変更が他の層へ波及する経路、といった論点で詰まりやすい。Level 2 では、SPA/MPA や認証フローの選択のように、複数の手段を比較して説明できるかを問う。

Level 2 全体の要約文には、クライアントとサーバを通した Web システムとして読む視点を置く（第 2 節の一行概要表）。個別の観点（通信・状態・検証など）は、第 2 節のキーワード一覧で分担する。

### 3. AI 時代：変更の波及を読む力を測る

方針： 仕様の暗記だけでは差別化しにくいため、ある実装が別の仕様・パフォーマンス・セキュリティへどう波及するかを読めるかを重視する。AI に任せると見落とされやすい CSP・認証フロー・CORS や、リフローなどのパフォーマンス副作用が典型例である。

出題しないもの： 特定フレームワーク・ライブラリの操作手順、仕様の丸暗記。フレームワークの選定根拠は問うが、フレームワーク固有の API は問わない。

---

## 2. 各試験の概要と技術領域の割り当てイメージ

試験のラインナップを、マークアップ Level 1、プログラミング Level 1、Level 2、という3つであると考えた場合に、その3試験のカバー範囲の案を示す。

### 一行概要

| 試験              | 一行概要                                                                 |
| --------------- | -------------------------------------------------------------------- |
| マークアップ Level 1  | 通信・文書・スタイル・表示環境の各指定が及ぼす影響を読み、選択や判断ができる。                              |
| プログラミング Level 1 | プログラムの構造とデータを理解し、モジュール構成・インタラクション・通信とセキュリティの前提を踏まえて判断できる。            |
| レベル2（試験横断・仮）    | クライアントとサーバを通した Web システムについて、通信・状態・検証・品質の各観点で要素技術を比較し、全体としての設計判断ができる。 |

### キーワード割当て

Web の技術や業務のキーワードがどの試験に割り当てられるか、を表の形で整理する。各試験の詳細な出題範囲を決める際のたたき台とする。各セル先頭の記号は以下の通り。

- ◎ ... その試験でその行のキーワードが主題タイトルレベルで掲載される想定
- ○ ... 主題レベルで掲載はされない見込みだが、この行のキーワードに関連する内容が出題範囲のどこかで現れる想定
- (記号なし ... その試験ではそのキーワードに直接関連する要素は出ない想定)

| キーワード | マークアップ L1 | プログラミング L1 | レベル2 |
|---|---|---|---|
| 通信 | ◎ リクエスト・レスポンス、フォーム送信（`method` / `action` / `enctype`）、HTTPS — 文書・フォームから見える HTTP | (非同期処理とセットにした主題で言及) | ○ Fetch API による詳細な通信制御、サーバ側 API の境界と責務 |
| セマンティクス | ◎ 要素・属性の意味、見出し階層、ランドマーク | | ○ マイクロデータ / Schema.org、`figure` / `time`、OG メタ、`hreflang` など |
| プレゼンテーション | ◎ セレクタ・カスケード・詳細度、基本レイアウト（ボックスモデル・配置、Flexbox/Grid の基礎）、見た目の追跡 | | ○ 複雑レイアウト・アニメーション・トランジション |
| インタラクション | ◎ フォーム、擬似クラス（`:hover` 等）、`transition`、`cursor`、`outline` 等 | ◎ イベント、DOM、Selectors API、フォームの操作、基本的なウィンドウ操作 | ○ DOM 応用、History API |
| 環境差 | ◎ viewport、メディアクエリ、レスポンシブ | | ○ コンテナクエリ、CSS Logical Properties、ウィンドウオブジェクトの寸法・スクロール |
| メディア | ◎ 画像・音声・動画の埋め込みと配信最適化（メディア要素、形式選択、`srcset` / `sizes`、`preload`、`poster`、`width` / `height`）、Webフォントの読み込み・表示制御（`@font-face`、WOFF2、`font-display`） | | ◎ メディア要素の API（`HTMLMediaElement`）、Canvas(2D)、SVG、アニメーションフレーム、`getUserMedia` / MediaStream、MSE/EME、HLS/DASH、可変フォント（`font-variation-settings`、`unicode-range`） |
| プログラムの基本構造 | | ◎ JavaScript 文法、型・制御・オブジェクト・クラス骨格 | ○ 高度な関数とコレクション操作、Proxy、generator |
| コレクションとデータ | | ◎ 配列の走査・変換・集約、JSON、文字列・RegExp | ○ Map/Set 等 |
| デバッグ | | ○ Console、ブレークポイント、実行時間計測 | |
| モジュールと配布 | | ◎ ES Modules、依存・`package.json`、バンドルと配布の流れ | ○ モジュールとパッケージ管理、ポリフィル、Babel/TypeScript |
| 非同期処理と通信 | | ◎ Promise・async/await、Fetch API、タイマー | ○ 高度な非同期・並行パターン、非同期ジェネレータ |
| セキュリティ | ○ HTTPS・混在コンテンツ、別オリジン埋め込み（`iframe`） | ◎ セキュアコンテキスト、オリジン、API 権限、依存の取り込み | ◎ クロスオリジン制約と CORS、セキュリティモデルと SSL、認可境界、サプライチェーン |
| 状態管理 | ○ フォームの宣言的な状態 | ○ Web Storage、UI 状態、非同期完了後の更新 | ◎ クライアント・サーバ・キャッシュの一貫性、競合・二重送信 |
| バリデーション | ○ HTML 制約属性 | ○ 制約検証 API、送信前チェック | ◎ 層ごとの検証分担、信頼境界、API 契約・スキーマ、表示とサーバデータの対応、API エラーと UI の整合 |
| 認証・認可 | ○ ログインフォーム | ○ トークン付きリクエストの読解 | ◎ セッション・トークン・Cookie、フロントとサーバの分担 |
| アクセシビリティ | ○ 代替テキスト等 | | ◎ 要件に応じた a11y 手法、キーボード・ARIA |
| パフォーマンス | | ○ High Resolution Time | ◎ Web Workers、Page Visibility、Navigation Timing / Resource Timing、読み込み・描画・ネットワークの最適化 |
| 再利用・構成 | | | ◎ CSS カスタムプロパティと関数、CSS ネスティング、カスケードキーワードの使い分け、Web コンポーネント、Shadow DOM |
| 用途別 Web API | | | ◎ リアルタイム：WebSocket、Server-Sent Events、WebRTC、Push API／ストレージ・ファイル：Indexed Database API、File API、バイナリーデータ、Service Worker（旧オフラインアプリケーション API）／デバイス：Geolocation API |

表を作るに当たって、行には「業務課題」くらいの抽象度のキーワードが MECE に並ぶことを目指している: 

- JavaScript、～～要素、～～API というワード (=具体的に扱う対象) よりも、業務や解決すべき課題のワードを優先する。基本的にこの認定は実現手段そのものよりは仕組みの理解を確認するものであるため。ただし実際の出題において扱う対象が JavaScript などに偏ることを否定するものではない。
- より総称的なキーワード (例、設計判断、バックエンド、可用性、保守性、...) は主題タイトルよりも上位の説明、例えば認定の概要説明や確認できるスキルレベルのところで必要があれば言及する。
- 結果的に現行試験と同等の内容群が出題範囲に残る場合でも、主題の切り方は変わる場合がある。例えば現行 Level 2 では多くの Web API を種類ごとに主題に並べている部分があるが、多くのアプリで使われる相対的に汎用なものはプログラミング Level 1 に割り当て、残りを Level 2 に割り当てるとすると、Level 2 に割り当たるものは他キーワードと均等なサイズに区切ることを考えると、「用途別 Web API」というようなグルーピングになると思われる。

## 3. 現行試験からの主要な廃止項目案

現行 ver.2.5（`objectives_level1.md` / `objectives_level2.md`）の出題範囲のうち、改訂で取り下げる見込みの項目。`decision_records.md` で判断済みのうち、仕様廃止・非推奨・主要実装の欠如など客観的理由があり、企画段階で争点になりにくいもののみ列挙する。節の再編や別キーワードへの吸収（`Console` 全般の扱い、`2.2.6` テスト・デバッグなど）は本表の対象外とする。

| 内容 | 現行試験の主題・副題 | 理由（簡潔） |
|---|---|---|
| Fluid Grid（フルードグリッド） | L1 1.4.1 | Web 標準の技術ではなく、抽象的概念のため |
| Generic Sensor API | L1 1.5.2 | 主要ブラウザで未実装のため |
| DeviceOrientation / DeviceMotion Event | L1 1.5.2、L2 2.7.2 | 現行 Web 開発での重要性が低下したため |
| XMLHttpRequest | L1 1.5.4、L2 2.6.2 | `fetch` で代替可能なため |
| `with` 文 | L2 2.1.1 | 言語仕様上非推奨のため |
| `console.profile()` / `console.profileEnd()` | L2 2.2.6 | 非標準 API のため |
| `File.lastModifiedDate` | L2 2.5.3 | 非推奨 API のため（`lastModified` を用いる） |
| Application Cache（`ApplicationCache`） | L2 2.8.3 | 仕様廃止。Service Worker へ置き換え |

---

## 4. 出題範囲の設計原則

出題範囲の詳細は `objectives_markup.md` / `objectives_programming.md` に記載する。各主題を範囲に入れるかどうかの判断根拠は `decision_records.md` に残す。

範囲は「この技術を知らないと、実務でどんな判断ができないか」で切る。仕様を漏れなく載せることより、判断の根拠になるかを優先する。

---

## 5. 検討中の課題

- Level 2 の試験・認定の分割と、第 2 節「レベル2」列の対応付け
- 第 2 節と各試験ファイル（一行概要・見出し・出題範囲）の同期
- プログラミング Level 1 の見出し 6 本分への改訂反映
- 認証・認可を独立見出しとするか、セキュリティへ統合するか
- 観点の過不足、AI 任せにできない領域の境界

第 2 節に仮置き済み（`objectives_programming.md` 未反映）： 旧 L1「API の基礎知識」のドメイン概要はレベル2 用途別 Web API 行に集約。廃止項目は第 3 節。

