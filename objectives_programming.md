# Web プロフェッショナルプログラミング認定 <!-- omit in toc -->

## 名称 <!-- omit in toc -->

| | 日 | 英 |
|---|---|---|
| **認定名** | Web プロフェッショナルプログラミング Level 1 認定 | Web Professional Programming Level 1 Certification |
| **試験名** | Web プロフェッショナルプログラミング Level 1 試験 | Web Professional Programming Level 1 Exam |

## 認定条件 <!-- omit in toc -->

- Web プロフェッショナル プログラミング Level 1 試験に合格すると Web プロフェッショナル プログラミング Level 1 に認定されます。

## 確認できるスキルレベル <!-- omit in toc -->

基本的なプログラミングやデータ操作について理解し、DOM 操作やサーバとの非同期通信を含む動的な Web アプリケーションを、モジュール構成やセキュリティを踏まえて実装できる。

- **プログラムの基本構造** — JavaScript の文法・型・制御構文を理解し、スクリプトの実行結果とその影響を予測できる。
- **コレクションとデータ** — 配列の走査・変換・集約や JSON、文字列操作を理解し、目的に応じて適切なメソッドやデータ構造を選択してデータを加工・抽出できる。
- **動的 Web ページの構成要素** — HTML・CSS・JavaScript・通信の役割分担とクライアント・サーバの分担を理解し、スクリプトが組み込まれた HTML 文書の構成と実行の流れを読み取れる。
- **モジュールと配布** — ES Modules やパッケージ管理、バンドルと配布の流れを理解し、外部依存を含むソースセットのビルド・デプロイができる。
- **操作への応答** — イベントの発生・伝播と DOM 操作の仕組みを理解し、ユーザー操作に応じた画面の変化を実装できる。
- **入力と状態保持** — フォーム送信に関して、入力値の取得と動的検証、入力・送信イベントの操作、状態保存など、関係する一連の処理を実装・確認できる。
- **非同期処理と通信** — 非同期処理の仕組みと制約を理解した上で、Fetch などの Web API を用いたブラウザ・サーバー間通信をセキュリティ制約やエラーへの対応も含めて実装できる。
- **セキュリティ** — 実行コンテキストや同一オリジン内の到達範囲、認証・セッション保護の仕組みを理解し、情報露出やリスクを回避して安全に実装できる。

# 目次 <!-- omit in toc -->
- [Web プロフェッショナルプログラミング Level 1 試験 出題範囲](#web-プロフェッショナルプログラミング-level-1-試験-出題範囲)
  - [1. プログラムの基本構造](#1-プログラムの基本構造)
    - [1.1. 文法・型・演算子 (重要度: 4) / 旧 2.1.1](#11-文法型演算子-重要度-4--旧-211)
    - [1.2. 制御構造と例外処理 (重要度: 4)](#12-制御構造と例外処理-重要度-4)
    - [1.3. オブジェクトとクラス (重要度: 3) / 旧 2.1.2](#13-オブジェクトとクラス-重要度-3--旧-212)
  - [2. コレクションとデータ](#2-コレクションとデータ)
    - [2.1. 配列とコレクション操作 (重要度: 4)](#21-配列とコレクション操作-重要度-4)
    - [2.2. JSON・文字列・RegExp (重要度: 3)](#22-json文字列regexp-重要度-3)
  - [3. 動的 Web ページの構成要素](#3-動的-web-ページの構成要素)
    - [3.1. 構成技術とクライアント・サーバの役割分担 (重要度: 2)](#31-構成技術とクライアントサーバの役割分担-重要度-2)
    - [3.2. スクリプトの読み込みと実行 (重要度: 2)](#32-スクリプトの読み込みと実行-重要度-2)
  - [4. 開発ツールチェーン](#4-開発ツールチェーン)
    - [4.1. パッケージと配布の流れ (重要度: 3)](#41-パッケージと配布の流れ-重要度-3)
    - [4.2. デバッグと実行時間計測 (重要度: 2)](#42-デバッグと実行時間計測-重要度-2)
  - [5. 操作への応答](#5-操作への応答)
    - [5.1. DOM の基本操作 (重要度: 5) / 旧 2.2.2・旧 2.2.4](#51-dom-の基本操作-重要度-5--旧-222224)
    - [5.2. イベントの基本 (重要度: 2) / 旧 2.2.1](#52-イベントの基本-重要度-2--旧-221)
    - [5.3. 基本的なウィンドウ操作 (重要度: 2) / 旧 2.2.3 (一部)](#53-基本的なウィンドウ操作-重要度-2--旧-223-一部)
  - [6. 入力と状態保持](#6-入力と状態保持)
    - [6.1. フォーム入力の取得と検証 (重要度: 2) / 旧 2.2.2 (一部)](#61-フォーム入力の取得と検証-重要度-2--旧-222-一部)
    - [6.2. フォーム送信の制御とデータ収集 (重要度: 2) / 旧 2.2.2 (一部)](#62-フォーム送信の制御とデータ収集-重要度-2--旧-222-一部)
    - [6.3. クライアントサイドでの状態保持 (重要度: 2) / 旧 2.5.1 (一部)](#63-クライアントサイドでの状態保持-重要度-2--旧-251-一部)
  - [7. 非同期処理と通信](#7-非同期処理と通信)
    - [7.1. 繰り返しと時間指定実行 (重要度: 2) / 旧 2.2.3 (一部)](#71-繰り返しと時間指定実行-重要度-2--旧-223-一部)
    - [7.2. Promise, async/await (重要度: 3)](#72-promise-asyncawait-重要度-3)
    - [7.3. Fetch API による通信 (重要度: 3)](#73-fetch-api-による通信-重要度-3)
  - [8. ブラウザセキュリティと認証](#8-ブラウザセキュリティと認証)
    - [8.1. 実行コンテキストと同一オリジンポリシー (重要度: 2)](#81-実行コンテキストと同一オリジンポリシー-重要度-2)
    - [8.2. クロスオリジン通信と CORS (重要度: 3)](#82-クロスオリジン通信と-cors-重要度-3)
    - [8.3. パスワード認証とセッション保護 (重要度: 2) / 旧 1.1.1 (一部)・L2 11.1 (一部)](#83-パスワード認証とセッション保護-重要度-2--旧-111-一部l2-111-一部)
    - [8.4. ブラウザ機能の利用権限とデバイスアクセス (重要度: 1) / 旧 1.5.2](#84-ブラウザ機能の利用権限とデバイスアクセス-重要度-1--旧-152)


# Web プロフェッショナルプログラミング Level 1 試験 出題範囲

## 1. プログラムの基本構造

### 1.1. 文法・型・演算子 (重要度: 4) / 旧 2.1.1
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- JavaScript の基本文法・データ型・演算子を理解し、正しく記述し問題点を修正できる。

#### 詳細
- 基本構文を正しく記述する。
  - 文と式の違い
  - スコープの概念と変数や関数宣言の挙動
  - コメントの構文（単一行 `//`、複数行 `/* */`）
  - コード整形に関するルール（セミコロン、改行とインデント）
- データ型と変数を使い分ける。
  - プリミティブ型 (文字列, 数値, 真偽値, `null`, `undefined`)
  - オブジェクト型 (オブジェクト, 配列, 関数)
  - 変数の宣言 (`var`, `let`, `const`) とスコープ、再代入の挙動
  - 型に関する演算子: `instanceof`, `typeof`
- テンプレートリテラルを利用する。
- 個々の演算子の挙動の違いを理解している。
  - 算術演算子(`+`, `-`, `*`, `/`, `%`)
  - 比較演算子(`==`, `===`, `!=`, `!==`, など)
  - 論理演算子(`&&`, `||`, `!`)
  - 代入演算子(`=`, `+=`, など)
  - オプショナルチェーン(`?.`), null合体演算子(`??`)

### 1.2. 制御構造と例外処理 (重要度: 4)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 条件分岐・ループ・例外処理を把握し、制御の流れを読み取って適切に使い分けられる。

#### 詳細
- 目的に合った条件分岐構文を選択できる。
  - `if`, `else if`, `else`, `switch`
- 各種ループ構文を使い分けと制御方法を理解している。
  - `for`, `while`, `do/while`, `for ... in`, `for ... of`
  - `break` と `continue`
- 例外処理によりエラーを制御する。
  - `try/catch`, `throw`, `finally`

### 1.3. オブジェクトとクラス (重要度: 3) / 旧 2.1.2
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- オブジェクト・クラスの基本的な概念と操作、関連する構文を理解し、活用できる。

#### 詳細
- オブジェクトの概念を理解し、プロパティを操作できる。
  - オブジェクトの作成、プロパティ定義・アクセス
  - メソッドの定義
  - アロー関数 (`() => {}`) の構文と従来の関数との違い
- オブジェクトの展開と代入に関する構文を利用できる。
  - スプレッド構文 (`...`)
  - 残余引数
  - 分割代入
- クラスやインスタンスを設計・利用する。
  - class構文とconstructor
  - インスタンス生成、継承

## 2. コレクションとデータ

### 2.1. 配列とコレクション操作 (重要度: 4)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 配列の走査・変換・集約など、コレクション操作の基本的な方法を理解し、データ処理に活用できる。

#### 詳細
- 配列データの構造を概念的に理解している。
- 配列を各種メソッドを用いて操作する。
  - アクセス、`length`
  - 変更メソッド: `push()`, `pop()`, `shift()`, `unshift()`, `splice()`, `concat()`, `slice()`
  - 反復処理メソッド:（`map()`, `filter()`, `find()`, `reduce()`, `forEach()`
  - 検索と存在確認:（`indexOf()`, `includes()`, `some()`, `every()`

### 2.2. JSON・文字列・RegExp (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- JSON や文字列、正規表現など、データの表現と変換に関する組み込みオブジェクトを理解し、適切に活用できる。

#### 詳細
- JSON とオブジェクトを相互変換する。
  - `JSON.parse()`, `JSON.stringify()`
  - JSON フォーマットのルールと JavaScript オブジェクトリテラルとの違い
- String オブジェクトのメソッドを用いて文字列を操作する。
- RegExp による正規表現パターンマッチの一連の操作の流れを理解している。
- その他の主要な組み込みオブジェクト（Boolean, Date, Error, Math, Number, Object）の概要を理解している。

## 3. 動的 Web ページの構成要素

<details><summary>メモ</summary><div>

- JavaScript 基礎以降の出題内容の理解には、マークアップ領域の基礎知識を要するものがある。一方で本試験はマークアップ認定の取得を前提とはしない。そのため、プログラミング領域に関連が深い HTML/CSS 領域を概観する目的で本主題を設置する。

</div></details>

### 3.1. 構成技術とクライアント・サーバの役割分担 (重要度: 2)
出題種別: 知識問題

#### 概要
- 動的 Web ページの機能が HTML・CSS・JavaScriptおよび通信の組み合わせで実現されることを理解し、個々の仕様や実装すべき機能がどれに関わるものかを識別できる。

#### 詳細
- 機能の実現・変更に関わる技術の役割分担を識別できる。
  - 構造（HTML）・見た目（CSS）・振る舞い（JavaScript）・データのやりとり（通信）
  - ある機能の変更や不具合が、どの層に属するかの切り分け
- クライアントサイドで完結する機能と、サーバーサイドの処理を要する機能を区別できる。
  - データがブラウザの外に残る必要があるか、他の利用者や別の端末に影響が及ぶか

### 3.2. スクリプトの読み込みと実行 (重要度: 2)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- HTML 文書への JavaScript の組み込みについて、記載方法と挙動の対応を理解している。
- `import` / `export` によるソースの分割と、モジュールスコープの違いが及ぼす影響を理解している。

#### 詳細
- `script` 要素による JavaScript 組み込みについて、記述位置や属性の種類と挙動を理解し使い分ける。
  - インラインスクリプトと外部ファイルの読み込み
  - `head` 内と `body` 末尾
  - `defer`, `async`
- モジュール形式でスクリプトを取り込む。classic script との違いが及ぼす影響を読み取る。
  - `type="module"`
  - モジュールスコープとグローバルスコープ
  - `import`, `export`

## 4. 開発ツールチェーン

### 4.1. パッケージと配布の流れ (重要度: 3)
出題種別: 知識問題、記述問題

#### 概要
- フロントエンド開発におけるパッケージ管理・バンドル・配布などの各工程の必要性や大まかな処理の流れを理解している。

#### 詳細
- 開発ツールチェーンを動かすランタイムとしての JavaScript エンジンを認識している。
- パッケージを取得し依存関係を記録する。
  - `npm install`
  - `package.json`, `package-lock.json`
- 開発中のスクリプトを実行する環境を立ち上げる。
  - `npm run`
  - 開発サーバ、ホットリロード
- バンドル処理の成果物と元ソースの違いを認識している。
- 外部パッケージ取り込み時のセキュリティ確認ができる。
  - サードパーティ製ライブラリの信頼境界の認知
  - 開発工程中のアタックサーフェスの認知: フックスクリプトなど
  - 既知の脆弱性の確認: `npm audit`

### 4.2. デバッグと実行時間計測 (重要度: 2)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 基本的なデバッグツールを利用しエラーを調査できる。
- コードの実行時間を計測する標準的な方法（High Resolution Time を含む）を理解し、適用できる。

#### 詳細
- コンソールを使って動作確認を行う。
  - `console.log` - 基本的なログ出力
  - `console.info()`, `console.warn()`, `console.error()` - 重要度別のログ出力
  - `console.table()` - 表形式での構造化データの出力
  - `console.group()`, `console.groupEnd()` - ログのグループ化
  - `console.count()` - 呼び出し回数のカウント
  - `console.trace()` - スタックトレースの出力
  - `console.assert()` - 条件付きエラーログの出力
- コードの実行時間を計測する。
  - `console.time()`, `console.timeEnd()`, `console.timeLog()` - コンソールベースのタイミング計測
  - `Date.now()`, `new Date().getTime()` - ミリ秒単位の時間計測
  - `performance.now()` - High Resolution Time による高精度タイムスタンプの取得
- ブラウザの開発者ツールを用いて調査を行う。
  - 開発者ツールのコンソールタブによるデータ確認やコード断片のテスト実行
  - 開発者ツールのソースタブによるスクリプトのブレーク、ステップ実行、変数、コールスタックの確認
  - 開発者ツールのネットワークタブによる通信の観察（リクエスト・レスポンスの内容、キャッシュ由来かの確認）
  - debugger ステートメントの埋め込み

## 5. 操作への応答

### 5.1. DOM の基本操作 (重要度: 5) / 旧 2.2.2・旧 2.2.4
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- ユーザ操作を契機に DOM を更新する流れでは、5.2 節のイベント登録と組み合わせて用いる。

</div></details>

#### 概要
- DOM や Selectors API を利用して、HTML 要素の検索・取得や内容・構造の操作ができる。

#### 詳細
- 操作対象となる HTML 文書の基本構造を読める。
  - タグ・属性・入れ子の読解、`id`・`class` による要素の特定
- DOM の基本構成を理解している。
  - ツリー構造
  - `Document` インターフェース, `Element` インターフェース
  - `document` オブジェクト
- 要素を検索・取得する。
  - `getElementById`, `getElementsByClassName`, `getElementsByName`, `getElementsByTagName`
  - `querySelector`, `querySelectorAll`
- 要素を追加・削除する。
  - `createElement`, `insertBefore`, `appendChild`
- 要素の内容を取得・変更する。
  - `innerHTML`
  - `createAttribute`, `hasAttribute`, `removeAttribute`
- カスタムデータ属性を活用できる。
  - `dataset` プロパティによる `data-*` 属性値の取得・設定
  <!-- マークアップ試験 2.2 と重複掲載 -->

### 5.2. イベントの基本 (重要度: 2) / 旧 2.2.1
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- フォーム入力・送信におけるイベントの選び方と適用は 6.1・6.2 節で扱う。
- スクリプトの記述位置・読み込み指定による実行タイミングは 3.2 節。読み込み完了を契機にした処理の登録は本節。
- カスタムイベントの作成・発行は Level 2（DOM 応用）で扱う。
- ドロップで得たファイルを `File` / `FileReader` で読む流れは Level 2（14.2 節）で扱う。

</div></details>

#### 概要
- ユーザ操作や読み込みに伴うイベントの発生と伝播を読み取り、適切なタイミングで処理を登録できる。

#### 詳細
- イベントの登録・ハンドラ呼び出し・解除の基本的な流れを理解している。
  - `addEventListener()`, `removeEventListener()`
  - イベントオブジェクト（`target` 等）
- ある操作に対し、どの要素が対象となり、どのように伝播しうるかを読み取れる。
  - バブリング、イベント委譲
- 既定動作の有無と、`preventDefault()` で止められるもの／止められないものを区別できる。
- 入力や DOM の変化との前後関係を踏まえ、処理を載せるイベントを選べる。
- 代表的な処理の契機と典型系的な操作例の組み合わせを認知している。
  - 読み込みの完了: `DOMContentLoaded`, `load`
  - マウス・ポインタ操作: `click`, `dblclick`, `mousedown`, `mouseup`, `mousemove`, `mouseover`, `mouseout`, `pointerdown`, `pointerup`, `pointermove`
  - キー入力の段階: `keydown`, `keyup`
  - スクロール・ホイール: `scroll`, `wheel`
  - タッチ操作: `touchstart`, `touchmove`, `touchend`
  - ドラッグ＆ドロップ: `dragstart`, `drag`, `dragenter`, `dragover`, `dragleave`, `drop`, `dragend`

### 5.3. 基本的なウィンドウ操作 (重要度: 2) / 旧 2.2.3 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- スクロール位置の操作 API は本節。`scroll` / `wheel` イベントの登録は 5.2 節。Observer との使い分けは Level 2。
- `window.postMessage` による閲覧コンテキスト間のメッセージングは Level 2（8.1 節）。

</div></details>

#### 概要
- ウィンドウオブジェクト (`window`) に直接実装されている主要な機能を使いこなせる。

#### 詳細
- ウィンドウの座標・大きさを確認する。
  - `innerWidth`, `innerHeight`, `outerWidth`, `outerHeight`
  - `screen`, `screenX`, `screenY`
- スクリプトからスクロール処理を実行する。
  - `scroll` (`scrollTo`), `scrollBy`
- メッセージダイアログを表示する。
  - `alert()`, `confirm()`, `prompt()`
- URL を新しいウィンドウ／タブで開ける。
  - `open`

## 6. 入力と状態保持

### 6.1. フォーム入力の取得と検証 (重要度: 2) / 旧 2.2.2 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- フォーム入力値の取得と検証を、JavaScript から行える。

<details><summary>メモ</summary><div>

- フォーム要素・属性の宣言的な記述はマークアップ試験（5.1・5.2 節）で扱う。
- イベントの伝播・既定動作の一般則は 5.2 節。本節はフォーム入力への適用。

</div></details>

#### 詳細
- フォーム入力の値を取得する。
  - `element.value`, `element.checked`, `element.selectedIndex`
- 入力の変化に応じて処理を登録する。
  - 入力中の変化: `input`
  - 値の確定: `change`
  - フォーカスの移動: `focus`, `blur`
  - 検証失敗・選択: `invalid`, `select`
- 制約検証 API と、検証結果の表示制御を利用する。
  - `checkValidity()`, `reportValidity()`, `setCustomValidity()`
  - バリデーション属性（`required`, `pattern`, `minlength`, `maxlength` 等）
  - バリデーション擬似クラス（`:valid`, `:invalid`）

### 6.2. フォーム送信の制御とデータ収集 (重要度: 2) / 旧 2.2.2 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- `preventDefault()` の一般的な意味は 5.2 節。本節はフォーム送信の抑止への適用に限る。
- 宣言的な送信（`method` / `action`）はマークアップ試験（5.2 節）で扱う。

</div></details>

#### 概要
- フォーム送信の制御と、送信データの収集・保持を JavaScript から行える。

#### 詳細
- HTML フォーム要素を操作する。
  - `document.forms` コレクションによるフォーム要素へのアクセス
  - `form.submit()` と `form.reset()` メソッド
- 送信・リセットを契機にした処理と、既定の送信動作の制御を行う。
  - `submit`, `reset` イベント
  - `preventDefault()` による既定送信の抑止
- `FormData` オブジェクトを直接利用してデータを操作する。
  - フォームからのデータ収集 (`new FormData(formElement)`)
  - プログラムによるデータ追加 (`formData.append()`)

### 6.3. クライアントサイドでの状態保持 (重要度: 2) / 旧 2.5.1 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- 同一オリジン内のスクリプトから無条件に読める性質（機密情報を Web Storage に置かない判断）は 8.3 節で扱う。
- 保存先（Cookie・Web Storage・IndexedDB・メモリ・サーバ）の比較・選択や状態一貫性の設計、トークン保存場所の露出差は Level 2 で扱う。

</div></details>

#### 概要
- Web Storage（`localStorage`, `sessionStorage`）を用いて、入力データや UI 状態をブラウザ内に保持・取得・削除できる。

#### 詳細
- Web Storage によりオリジン単位でブラウザ内にデータを保持する。
  - `localStorage` と `sessionStorage` の生存期間（永続 vs タブセッション）と共有範囲の違い
  - `setItem()`, `getItem()`, `removeItem()` による値の保存・取得・削除
  - 文字列型での保存と、オブジェクト・配列を扱うための JSON 変換（`JSON.stringify()`, `JSON.parse()`）
  - 入力途中の下書き保存や UI 状態の復元への適用

## 7. 非同期処理と通信

### 7.1. 繰り返しと時間指定実行 (重要度: 2) / 旧 2.2.3 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 繰り返し処理や時間指定実行を実装できる。

#### 詳細
- `setInterval` を利用した繰り返し処理を実行、解除する。
- `setTimeout` を利用した指定時間後の処理を実行する。
- 典型的な利用シーンに対して個々の処理の実行順序などを読み取る。

### 7.2. Promise, async/await (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 非同期処理の基本概念と実装方法を理解し、効率的に非同期処理を行うことができる。

#### 詳細
- 非同期処理の基本概念を理解し、`Promise` や `async/await` で実装する。
- 非同期処理の状態に応じて画面表示を切り替える。
  - 読み込み中・成功・失敗の各状態を画面表示に反映する基本パターン

### 7.3. Fetch API による通信 (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- HTTP のプロトコル基礎やフォームによる送信はマークアップ試験（1.1・5.2 節）で扱う。
- ヘッダによって制御され得る挙動（キャッシュ可否など）の概要はマークアップ試験（1.3 節）でも扱う。両試験の前提として重複してよい。
- 開発者ツールでレスポンスがキャッシュ由来かを確認する切り分けは、4.2 節（デバッグ）と合わせて出題できる。

</div></details>

#### 概要
- サーバーとの非同期通信を実現する Fetch API の基本的な仕組みを理解し、一つのリクエストを正しく成立させて結果を処理できる。その過程でブラウザが既定で行う振る舞い（キャッシュの利用など）を知り、症状から説明・切り分けられる。
- 要件に応じて通信の振る舞いを既定から変えて制御すること（キャッシュモードの選択、認証情報の付与、中断・タイムアウト、ストリーミングレスポンスの逐次消費など）は Level 2（9.2 節）で扱う。
- セキュアコンテキストやクロスオリジン制約が通信に与える影響は 8 主題と合わせて理解する。

#### 詳細
- `fetch()` 関数のリクエストを設定する。
  - メソッドの指定: GET, POST, PUT, DELETE
  - ヘッダーの設定
  - リクエストボディの構築: JSON, FormData
- `fetch()` 関数のレスポンスとエラーを処理する。
  - ネットワークエラーの捕捉
  - HTTP ステータスの確認: `response.ok`, `response.status`
  - レスポンスボディの取得: `.json()`, `.text()`, `.blob()`
- ブラウザの既定動作としてレスポンスがキャッシュされうることを理解し、取得結果がキャッシュ由来かを切り分ける。
  - サーバ側のキャッシュ指示（`Cache-Control` 等）が挙動に関与することの概要

## 8. ブラウザセキュリティと認証

### 8.1. 実行コンテキストと同一オリジンポリシー (重要度: 2)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- スクリプトが動作する実行コンテキストとオリジンの概念を理解し、ブラウザが設ける境界とデータへの到達範囲を読み取れる。
- URL やクエリによる画面遷移がオリジン境界をまたぐ際の影響を理解している。

#### 詳細
- セキュアコンテキストの要件と影響を理解している。
  - HTTPS 環境、混在コンテンツ（Mixed Content）の制限
- オリジンの構成要素と同一オリジンポリシー（SOP）の原則を理解している。
  - スキーム、ホスト、ポート番号による同一オリジンの判定
  - 別オリジンのリソースに対するスクリプトからの読み取り制限
- 同一オリジン内で動作するスクリプトの到達範囲を理解している。
  - DOM、Cookie、Web Storage（`localStorage`, `sessionStorage`）へのアクセス権
  - サードパーティ製スクリプトが同一オリジンとして実行される影響
- 同一オリジンポリシーの対象外となる動作を理解している。
  - 画面遷移におけるオリジン境界の不適用と、外部入力値による遷移先の誤認

### 8.2. クロスオリジン通信と CORS (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 同一オリジンポリシーの制約下で別オリジンと通信する仕組み（CORS）を理解し、エラー発生時に原因を切り分けて、安全にリソースを共有するための設定や通信が行える。
- CORS が保護する対象と、CORS 単体では防げない脅威の違いを区別できる。

#### 詳細
- CORS によるアクセス制御の仕組みを理解している。
  - JavaScript からのレスポンス参照可否とサーバー側の処理実行の区別
  - レスポンスヘッダ（`Access-Control-Allow-Origin` 等）による許可指定
- プリフライトリクエストの仕組みと発生条件を理解している。
  - 単純リクエスト（Simple requests）と事前確認（`OPTIONS` メソッド）
  - プリフライト失敗時の要因切り分け（ヘッダ不足、ステータスエラー）
- クロスオリジン通信における認証情報の扱いを理解している。
  - Cookie 等の自動送信に関する既定動作
- CORS の適用範囲と防御の限界を理解している。
  - サーバー側の認可・アクセスコントロールとの責務分離
  - ブラウザを介さない直接通信や CSRF に対する CORS の非適用

### 8.3. パスワード認証とセッション保護 (重要度: 2) / 旧 1.1.1 (一部)・L2 11.1 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- パスワードによるログインからセッションが開始され、Cookie で維持される流れを理解し、認証情報の保護属性や保存場所の選択が及ぼす影響を読み取れる。

#### 詳細
- パスワード認証によりセッションが開始され、維持される仕組みを理解している。
  - ログインリクエストとセッション識別子の発行
  - ブラウザによる Cookie の自動付与とセッション維持
- セッション Cookie を保護する属性の用途と動作を理解している。
  - `Secure`, `HttpOnly`, `SameSite` 属性
- 機密情報の保存場所に応じたリスクの違いを理解している。
  - Web Storage と `HttpOnly` Cookie におけるスクリプトからの到達性の違い

### 8.4. ブラウザ機能の利用権限とデバイスアクセス (重要度: 1) / 旧 1.5.2
出題種別: 知識問題、記述問題

#### 概要
- カメラ・マイク・位置情報などデバイスアクセスを要する機能に共通する権限モデルとセキュリティ上の前提を理解している。

#### 詳細
- 利用権限を要するブラウザ機能の代表例を認識している。
  - カメラ・マイク（`navigator.mediaDevices.getUserMedia()`）、位置情報（Geolocation API）、通知（Notifications API）
- ブラウザの権限モデル（Permissions）の概要を理解し、ユーザーから許可を得る一連の処理を実装する。
  - 利用者への許可プロンプトと状態（`granted`, `denied`, `prompt`）
  - 拒否（`denied`）状態におけるスクリプトからの再要求の制限
