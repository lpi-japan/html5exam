# Web プロフェッショナルプログラミング認定 <!-- omit in toc -->

## 名称 <!-- omit in toc -->

| | 日 | 英 |
|---|---|---|
| **認定名** | Web プロフェッショナルプログラミング Level 1 認定 | Web Professional Programming Level 1 Certification |
| **試験名** | Web プロフェッショナルプログラミング Level 1 試験 | Web Professional Programming Level 1 Exam |

## 認定条件 <!-- omit in toc -->

- Web プロフェッショナル プログラミング Level 1 試験に合格すると Web プロフェッショナル プログラミング Level 1 に認定されます。

## 確認できるスキルレベル <!-- omit in toc -->

基本的なプログラミングやデータ操作について理解し、DOM 操作やサーバとの非同期通信を含む動的な Web アプリケーションを、モジュール構成やセキュリティを踏まえて実装できる。

- **プログラムの基本構造** — JavaScript の文法・型・制御構文を理解し、スクリプトの実行結果とその影響を予測できる。
- **コレクションとデータ** — 配列の走査・変換・集約や JSON、文字列操作を理解し、データの構造や変換が及ぼす影響を読み取って、利用場面を判断できる。
- **動的 Web ページの構成要素** — HTML・CSS・JavaScript・通信の役割分担とクライアント・サーバの分担を理解し、スクリプトが組み込まれた HTML 文書の構成と実行の流れを読み取れる。
- **モジュールと配布** — ES Modules やパッケージ管理、バンドルと配布の流れ、それらを支える開発の道具立てを理解し、依存や配布が及ぼす影響を読み取って、構成を判断できる。
- **操作への応答** — イベントの発生・伝播と DOM 操作の仕組みを理解し、ユーザー操作に応じた画面の変化を読み取れる。
- **入力とフォーム** — フォーム入力値の取得と制約検証 API による検証、入力・送信に伴うイベントの適用を理解し、入力の扱いが送信内容に与える影響を読み取れる。
- **非同期処理と通信** — 非同期処理の仕組みと制約、Fetch などの Web API を通じたクライアント・サーバ連携の前提（セキュアコンテキスト、CORS 等を含む）を理解し、待ち状態・エラー・ブラウザの既定動作（キャッシュの利用など）が生じる場面を見通して利用判断ができる。
- **セキュリティ** — 実行コンテキストや API 制約がスクリプトの振る舞いに与える影響を理解し、安全な利用の観点を踏まえて判断できる。

# 目次 <!-- omit in toc -->
- [Web プロフェッショナルプログラミング Level 1 試験 出題範囲](#web-プロフェッショナルプログラミング-level-1-試験-出題範囲)
  - [1. プログラムの基本構造](#1-プログラムの基本構造)
    - [1.1. 文法・型・演算子 (重要度: 4) / 旧 2.1.1](#11-文法型演算子-重要度-4--旧-211)
    - [1.2. 制御構造と例外処理 (重要度: 4)](#12-制御構造と例外処理-重要度-4)
    - [1.3. オブジェクトとクラス (重要度: 3) / 旧 2.1.2](#13-オブジェクトとクラス-重要度-3--旧-212)
  - [2. コレクションとデータ](#2-コレクションとデータ)
    - [2.1. 配列とコレクション操作 (重要度: 4)](#21-配列とコレクション操作-重要度-4)
    - [2.2. JSON・文字列・RegExp (重要度: 3)](#22-json文字列regexp-重要度-3)
  - [3. 動的 Web ページの構成要素](#3-動的-web-ページの構成要素)
    - [3.1. 構成技術とクライアント・サーバの役割分担 (重要度: 2)](#31-構成技術とクライアントサーバの役割分担-重要度-2)
    - [3.2. スクリプトの読み込みと実行 (重要度: 2)](#32-スクリプトの読み込みと実行-重要度-2)
  - [4. モジュールと開発基盤](#4-モジュールと開発基盤)
    - [4.1. ES Modules (重要度: 2)](#41-es-modules-重要度-2)
    - [4.2. パッケージと配布の流れ (重要度: 3)](#42-パッケージと配布の流れ-重要度-3)
    - [4.3. デバッグと実行時間計測 (重要度: 2)](#43-デバッグと実行時間計測-重要度-2)
  - [5. 操作への応答](#5-操作への応答)
    - [5.1. DOM の基本操作 (重要度: 2) / 旧 2.2.2](#51-dom-の基本操作-重要度-2--旧-222)
    - [5.2. Selectors API (重要度: 3) / 旧 2.2.4](#52-selectors-api-重要度-3--旧-224)
    - [5.3. イベントの基本 (重要度: 2) / 旧 2.2.1](#53-イベントの基本-重要度-2--旧-221)
    - [5.4. 基本的なウィンドウ操作 (重要度: 2) / 旧 2.2.3 (一部)](#54-基本的なウィンドウ操作-重要度-2--旧-223-一部)
  - [6. 入力とフォーム](#6-入力とフォーム)
    - [6.1. フォーム入力の取得と検証 (重要度: 2) / 旧 2.2.2 (一部)](#61-フォーム入力の取得と検証-重要度-2--旧-222-一部)
    - [6.2. フォーム送信の制御とデータ収集 (重要度: 2) / 旧 2.2.2 (一部)](#62-フォーム送信の制御とデータ収集-重要度-2--旧-222-一部)
  - [7. 非同期処理と通信](#7-非同期処理と通信)
    - [7.1. 繰り返しと時間指定実行 (重要度: 2) / 旧 2.2.3 (一部)](#71-繰り返しと時間指定実行-重要度-2--旧-223-一部)
    - [7.2. Promise, async/await (重要度: 3)](#72-promise-asyncawait-重要度-3)
    - [7.3. Fetch API による通信 (重要度: 3)](#73-fetch-api-による通信-重要度-3)
  - [8. セキュリティ](#8-セキュリティ)
    - [8.1. 実行コンテキストとオリジン (重要度: 3)](#81-実行コンテキストとオリジン-重要度-3)
    - [8.2. API 権限とデバイスアクセス (重要度: 2) / 旧 1.5.2](#82-api-権限とデバイスアクセス-重要度-2--旧-152)
    - [8.3. 依存の取り込み (重要度: 2)](#83-依存の取り込み-重要度-2)


# Web プロフェッショナルプログラミング Level 1 試験 出題範囲

## 1. プログラムの基本構造

### 1.1. 文法・型・演算子 (重要度: 4) / 旧 2.1.1
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- JavaScript の基本文法・データ型・演算子を理解し、正しく記述できる。

#### 詳細
- 基本構文を正しく記述できる。
  - 文と式の違い
  - スコープの概念と変数や関数宣言の挙動
  - コメントの構文（単一行 `//`、複数行 `/* */`）
  - コード整形に関するルール（セミコロン、改行とインデント）
- データ型と変数を正しく扱える。
  - プリミティブ型 (文字列, 数値, 真偽値, null, undefined)
  - オブジェクト型 (オブジェクト, 配列, 関数)
  - 変数の宣言 (var, let, const) とスコープ、再代入の挙動
  - 型に関する演算子: instanceof, typeof
- テンプレートリテラルを利用できる。
- 演算子と式を適切に使いこなせる。
  - 算術演算子(`+`, `-`, `*`, `/`, `%`)
  - 比較演算子(`==`, `===`, `!=`, `!==`, など)
  - 論理演算子(`&&`, `||`, `!`)
  - 代入演算子(`=`, `+=`, など)
  - オプショナルチェーン(`?.`), null合体演算子(`??`)

### 1.2. 制御構造と例外処理 (重要度: 4)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 条件分岐・ループ・例外処理を把握し、制御の流れを読み取って適切に使い分けられる。

#### 詳細
- 条件分岐の仕組みを把握し、柔軟に使い分けられる。
  - if, else if, else, switch文
- 各種ループ構文を正しく使い分けられる。
  - for, while, do-while, for...in, for...of
- ループの制御方法を使い分けられる。
  - `break`
  - `continue`
- 例外処理によりエラーを制御できる。
  - try-catch, throw, finally

### 1.3. オブジェクトとクラス (重要度: 3) / 旧 2.1.2
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- オブジェクト・クラスの基本的な概念と操作、関連する構文を理解し、活用できる。

#### 詳細
- オブジェクトの概念を理解し、プロパティを操作できる。
  - オブジェクトの作成、プロパティ定義・アクセス
  - メソッドの定義
  - アロー関数 (`() => {}`) の構文と従来の関数との違い
- オブジェクトの展開と代入に関する構文を利用できる。
  - スプレッド構文 (`...`)
  - 残余引数
  - 分割代入
- クラスやインスタンスを設計・利用できる。
  - class構文とconstructor
  - インスタンス生成、継承

## 2. コレクションとデータ

### 2.1. 配列とコレクション操作 (重要度: 4)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 配列の走査・変換・集約など、コレクション操作の基本的な方法を理解し、データ処理に活用できる。

#### 詳細
- 配列の基本操作（`length`、インデックスアクセス）ができる。
- 配列の変更操作（`push()`, `pop()`, `shift()`, `unshift()`, `splice()`, `concat()`, `slice()`）ができる。
- 配列の反復処理メソッド（`map()`, `filter()`, `find()`, `reduce()`, `forEach()`）を使い分けられる。
- 配列の検索と確認（`indexOf()`, `includes()`, `some()`, `every()`）ができる。

### 2.2. JSON・文字列・RegExp (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- JSON や文字列、正規表現など、データの表現と変換に関する組み込みオブジェクトを理解し、適切に活用できる。

#### 詳細
- JSON の変換とフォーマットの違いを理解している。
  - `JSON.parse()`, `JSON.stringify()`
  - JSON フォーマットのルールと JavaScript オブジェクトリテラルとの違い
- String オブジェクトによる文字列操作ができる。
- RegExp によるパターンマッチングの基礎を理解している。
- その他の主要な組み込みオブジェクト（Boolean, Date, Error, Math, Number, Object）の概要を理解している。

## 3. 動的 Web ページの構成要素

### 3.1. 構成技術とクライアント・サーバの役割分担 (重要度: 2)
出題種別: 知識問題

<details><summary>メモ</summary><div>

- 事例の列挙ではなく判断基準を示す書き方とし、具体的な機能の分類は問題側で問う。

</div></details>

#### 概要
- 動的 Web ページの機能が HTML・CSS・JavaScript・通信の組み合わせで実現されることを理解し、個々の機能について実現に必要な技術とクライアント・サーバの分担を識別できる。

#### 詳細
- 機能の実現・変更に関わる技術の役割分担を識別できる。
  - 構造（HTML）・見た目（CSS）・振る舞い（JavaScript）・データのやりとり（通信）
  - ある機能の変更や不具合が、どの層に属するかの切り分け
- クライアントサイドで完結する機能と、サーバーサイドの処理を要する機能を区別できる。
  - 判断の観点: データがブラウザの外に残る必要があるか、他の利用者や別の端末に影響が及ぶか
  - 例: 入力中の表示切り替えはクライアントで完結し、他の利用者への通知はサーバーを要する

### 3.2. スクリプトの読み込みと実行 (重要度: 2)
出題種別: 知識問題、コードリーディング問題

<details><summary>メモ</summary><div>

- `type="module"` による読み込みは 4.1 節で扱う。
- 読み込み完了を契機にリスナーを登録する話（`DOMContentLoaded` / `load`）は 5.3 節で扱う。

</div></details>

#### 概要
- HTML 文書への JavaScript の組み込み方と、記述位置や読み込み指定が実行タイミングに与える影響を読み取れる。

#### 詳細
- `script` 要素による JavaScript の組み込みを理解している。
  - インラインスクリプトと外部ファイルの読み込み
- 記述位置・読み込み指定が実行タイミングに与える影響を読み取れる。
  - `head` 内と `body` 末尾の違い（文書中の要素をスクリプトから参照できるかへの影響）
  - `defer`・`async` の概要

## 4. モジュールと開発基盤

### 4.1. ES Modules (重要度: 2)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- ES Modules によるソースファイルの分割と読み込みの仕組みを理解している。

#### 詳細
- `import` / `export` によるモジュールの定義と利用ができる。
- `<script type="module">` によるブラウザでの読み込みを理解している。
- モジュールスコープと従来のスクリプトの違いを理解している。

### 4.2. パッケージと配布の流れ (重要度: 3)
出題種別: 知識問題、記述問題

#### 概要
- フロントエンド開発におけるパッケージ管理と、バンドル・配布の流れを理解している。

#### 詳細
- npm によるパッケージの取得（`npm install`）ができる。
- `package.json` による依存関係の記述を理解している。
- 開発用スクリプトの実行（`npm run`、開発サーバとホットリロードの概要）を理解している。
- バンドルと配布の目的と流れ（ビルド成果物のブラウザ配信）を理解している。
- 依存の取り込みが及ぼす影響（セキュリティ・互換性の観点）を理解している。詳細は 8.3 節も参照。

### 4.3. デバッグと実行時間計測 (重要度: 2)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- v2.5 で独立主題だったデバッグは本主題の1節に統合した。単発のコーディングから複合的なアプリ開発へ視点を上げたときに現れる開発環境系の内容として、モジュール・パッケージ・開発サーバと同じ棚に置く（キーワード表でもデバッグは ○ 想定）。

</div></details>

#### 概要
- 基本的なデバッグツールを利用しエラーを調査できる。
- コードの実行時間を計測する標準的な方法（High Resolution Time を含む）を理解し、適用できる。

#### 詳細
- コンソールを使って動作確認とデバッグができる。
  - `console.log` - 基本的なログ出力
  - `console.info()`, `console.warn()`, `console.error()` - 重要度別のログ出力
  - `console.table()` - 表形式での構造化データの出力
  - `console.group()`, `console.groupEnd()` - ログのグループ化
  - `console.count()` - 呼び出し回数のカウント
  - `console.trace()` - スタックトレースの出力
  - `console.assert()` - 条件付きエラーログの出力
- コードの実行時間を計測できる。
  - `console.time()`, `console.timeEnd()`, `console.timeLog()` - コンソールベースのタイミング計測
  - `Date.now()`, `new Date().getTime()` - ミリ秒単位の時間計測
  - `performance.now()` - High Resolution Time による高精度タイムスタンプの取得
- ブラウザの開発者ツールを用いて調査できる。
  - 開発者ツールのコンソールタブによるデータ確認やコード断片のテスト実行
  - 開発者ツールのソースタブによるスクリプトのブレーク、ステップ実行、変数、コールスタックの確認
  - debugger ステートメントの埋め込み

## 5. 操作への応答

### 5.1. DOM の基本操作 (重要度: 2) / 旧 2.2.2
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- ユーザ操作を契機に DOM を更新する流れでは、5.3 節のイベント登録と組み合わせて用いる。

</div></details>

#### 概要
- DOM を利用して、HTML の要素を取得・操作できる。

#### 詳細
- 操作対象となる HTML 文書の基本構造を読める。
  - タグ・属性・入れ子の読解、`id`・`class` による要素の特定
- DOM の基本構成を理解している。
  - ツリー構造
  - Document インターフェース, Element インターフェース
  - document オブジェクト
- 要素を検索できる。
  - getElementById, getElementsByClassName, getElementsByName, getElementsByTagName
- 要素を追加・削除できる。
  - createElement, insertBefore, appendChild
- 要素の内容を取得・変更できる。
  - innerHTML
  - createAttribute, hasAttribute, removeAttribute
- カスタムデータ属性を活用できる。
  - `dataset` プロパティによる `data-*` 属性値の取得・設定
  <!-- マークアップ試験 2.2 と重複掲載 -->

### 5.2. Selectors API (重要度: 3) / 旧 2.2.4
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- Selectors APIを使い、効率的にHTML要素を操作できる。

#### 詳細
- Selectors API を使って HTML 要素を操作できる。
  - querySelector, querySelectorAll

### 5.3. イベントの基本 (重要度: 2) / 旧 2.2.1
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- フォーム入力・送信におけるイベントの選び方と適用は 6.1・6.2 節で扱う。
- スクリプトの記述位置・`defer`/`async` による実行タイミングは 3.2 節で扱う。文書やリソースの読み込み完了を契機にした処理の登録は本節。
- カスタムイベントの作成・発行は Level 2（DOM 応用）で扱う。
- ドロップで得たファイルを `File` / `FileReader` で読む流れは Level 2（14.2 節）で扱う。

</div></details>

#### 概要
- ユーザ操作や読み込みに伴うイベントの発生と伝播を読み取り、適切なタイミングで処理を登録できる。

#### 詳細
- イベントの登録・解除と、ハンドラに渡るイベントオブジェクトの基本を理解している。
  - `addEventListener()`, `removeEventListener()`
  - イベントオブジェクトが示す対象や種別の概要（`target` 等）
- ある操作に対し、どの要素が対象となり、どのように伝播しうるかを読み取れる。
  - バブリングの概要、親要素での一括処理（イベント委譲）の考え方
- 既定動作の有無と、`preventDefault()` で止められるもの／止められないものを区別できる。
- 入力や DOM の変化との前後関係を踏まえ、処理を載せるイベントを選べる。
- 代表的な契機に処理を登録できる。
  - 読み込みの完了: `DOMContentLoaded`, `load`
  - マウス・ポインタ操作: `click`, `dblclick`, `mousedown`, `mouseup`, `mousemove`, `mouseover`, `mouseout`, `pointerdown`, `pointerup`, `pointermove`
  - キー入力の段階: `keydown`, `keyup`
  - スクロール・ホイール: `scroll`, `wheel`
  - タッチ操作: `touchstart`, `touchmove`, `touchend`
  - ドラッグ＆ドロップ: `dragstart`, `drag`, `dragenter`, `dragover`, `dragleave`, `drop`, `dragend`

### 5.4. 基本的なウィンドウ操作 (重要度: 2) / 旧 2.2.3 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- スクロール位置の操作 API は本節。`scroll` / `wheel` イベントの登録は 5.3 節。Observer との使い分けは Level 2。

</div></details>

#### 概要
- ウィンドウオブジェクト (window) に直接実装されている主要な機能を使いこなせる。

#### 詳細
- ウィンドウの座標・大きさを確認できる。
  - innerWidth, innerHeight, outerWidth, outerHeight
  - screen, screenX, screenY
- スクロールを制御できる。
  - scroll (scrollTo), scrollBy
- メッセージダイアログを表示できる。
  - alert(), confirm(), prompt()
- URL を新しいウィンドウ／タブで開ける。
  - open

## 6. 入力とフォーム

### 6.1. フォーム入力の取得と検証 (重要度: 2) / 旧 2.2.2 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- フォーム入力値の取得と検証を、JavaScript から行える。

<details><summary>メモ</summary><div>

- フォーム要素・属性の宣言的な記述はマークアップ試験（5.1・5.2 節）で扱う。
- イベントの伝播・既定動作の一般則は 5.3 節。本節はフォーム入力への適用。

</div></details>

#### 詳細
- フォーム入力の値を取得できる。
  - `element.value`, `element.checked`, `element.selectedIndex`
- 入力の変化に応じて処理を登録できる。
  - 入力中の変化: `input`
  - 値の確定: `change`
  - フォーカスの移動: `focus`, `blur`
  - 検証失敗・選択: `invalid`, `select`
- 制約検証 API と、検証結果の表示制御を利用できる。
  - `checkValidity()`, `reportValidity()`, `setCustomValidity()`
  - バリデーション属性（`required`, `pattern`, `minlength`, `maxlength` 等）
  - バリデーション擬似クラス（`:valid`, `:invalid`）

### 6.2. フォーム送信の制御とデータ収集 (重要度: 2) / 旧 2.2.2 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

<details><summary>メモ</summary><div>

- `preventDefault()` の一般的な意味は 5.3 節。本節はフォーム送信の抑止への適用に限る。
- 宣言的な送信（`method` / `action`）はマークアップ試験（5.2 節）で扱う。

</div></details>

#### 概要
- フォーム送信の制御と、送信データの収集・保持を JavaScript から行える。

#### 詳細
- HTML フォーム要素を操作できる。
  - `document.forms` コレクションによるフォーム要素へのアクセス
  - `form.submit()` と `form.reset()` メソッド
- 送信・リセットを契機にした処理と、既定の送信動作の制御ができる。
  - `submit`, `reset` イベント
  - `preventDefault()` による既定送信の抑止
- FormData オブジェクトを利用できる。
  - フォームからのデータ収集 (`new FormData(formElement)`)
  - プログラムによるデータ追加 (`formData.append()`)
- Web Storage（`localStorage`, `sessionStorage`）による UI 状態の保持の概要を理解している。

## 7. 非同期処理と通信

### 7.1. 繰り返しと時間指定実行 (重要度: 2) / 旧 2.2.3 (一部)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 繰り返し処理や時間指定実行を理解し実装できる。

#### 詳細
- `setInterval` を利用した繰り返し処理を実行できる。
- `setTimeout` を利用した指定時間後の処理を実行できる。

### 7.2. Promise, async/await (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- 非同期処理の基本概念と実装方法を理解し、効率的に非同期処理を行うことができる。

#### 詳細
- 非同期処理の基本概念を理解し、Promise や async/await で実装できる。
  - Promise, async/await
- 非同期処理の状態に応じて画面表示を切り替えられる。
  - 読み込み中・成功・失敗の各状態を画面表示に反映する基本パターン

### 7.3. Fetch API による通信 (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- サーバーとの非同期通信を実現する Fetch API の基本的な仕組みと活用方法を理解する。
- 一つのリクエストを正しく成立させて結果を処理できる。その過程でブラウザが**既定で**行う振る舞い（キャッシュの利用など）を知り、症状から説明・切り分けできる。
- 要件に応じて通信の振る舞いを既定から変えて制御する（キャッシュモードの選択、認証情報の付与、中断・タイムアウト、ストリーミングレスポンスの逐次消費など）は Level 2（9.2 節）で扱う。
- セキュアコンテキストやクロスオリジン制約が通信に与える影響は 8 主題と合わせて理解する。

<details><summary>メモ</summary><div>

- HTTP のプロトコル基礎やフォームによる送信はマークアップ試験（1.1・5.2 節）で扱う。
- ヘッダによって制御され得る挙動（キャッシュ可否など）の概要はマークアップ試験（1.3 節）でも扱う。両試験の前提として重複してよい。
- 開発者ツールでレスポンスがキャッシュ由来かを確認する切り分けは、4.3 節（デバッグ）と合わせて出題できる。

</div></details>

#### 詳細
- `fetch()` 関数のリクエストを設定できる。
  - メソッドの指定: GET, POST, PUT, DELETE
  - ヘッダーの設定
  - リクエストボディの構築: JSON, FormData
- `fetch()` 関数のレスポンスとエラーを処理できる。
  - ネットワークエラーの捕捉
  - HTTP ステータスの確認: `response.ok`, `response.status`
  - レスポンスボディの取得: `.json()`, `.text()`, `.blob()`
- ブラウザの既定動作としてレスポンスがキャッシュされうることを理解し、取得結果がキャッシュ由来かを切り分けできる。
  - サーバ側のキャッシュ指示（`Cache-Control` 等）が挙動に関与することの概要

## 8. セキュリティ

### 8.1. 実行コンテキストとオリジン (重要度: 3)
出題種別: 知識問題、コードリーディング問題、記述問題

#### 概要
- スクリプトが動作する実行コンテキストとオリジンの概念を理解し、API の利用可否や通信の前提への影響を読み取れる。
- 同一オリジンポリシーと CORS の役割の違いを理解し、クロスオリジン通信で生じる制約とその意味を判断できる。

#### 詳細
- セキュアコンテキスト（HTTPS）の要件と、混在コンテンツが及ぼす影響を理解している。
- オリジン（スキーム・ホスト・ポート）の概念と、同一オリジンポリシーが守ろうとする脅威の概要を理解している。
  - 別オリジンのスクリプトがユーザーのブラウザ経由で他サイトのレスポンスを読み取る経路
- CORS が制御する対象（JavaScript からのレスポンス参照可否）と、制御しない対象（サーバーへの到達・サーバー側の処理）を区別できる。
  - 通信は成立してもスクリプトから結果を読めない場合があること
- CORS 設定だけでは防げない脅威と、CORS が担う防御の範囲を区別できる。
  - サーバー側の認可、CSRF、XSS、ブラウザ外からの直接呼び出し
- レスポンスヘッダによる公開先の指定の意味を理解している。
  - `Access-Control-Allow-Origin`、ワイルドカード指定の含意
- プリフライトリクエストが発生する条件と、失敗時の切り分けの概要を理解している。
- クロスオリジンのリクエストでは、既定では Cookie 等の資格情報が送られないことを理解している。
  - 資格情報を含める制御（`credentials`）とサーバ側の対応は Level 2（9.2 節・11.1 節）で扱う
- スクリプトの読み込み元が実行時の権限や振る舞いに与える影響を理解している。

### 8.2. API 権限とデバイスアクセス (重要度: 2) / 旧 1.5.2
出題種別: 知識問題、記述問題

<details><summary>メモ</summary><div>

- 本節は API 横断の権限モデルに限定する。個別 API の利用（MediaStream の操作、Geolocation 等）は Level 2 で扱う（境界基準に基づく整理。改訂概要 3.3 参照）。

</div></details>

#### 概要
- カメラ・マイク・位置情報などデバイスアクセスを要する API に共通する権限モデルとセキュリティ上の前提を理解している。

#### 詳細
- 権限を要する API の存在と代表例を理解している。
  - カメラ・マイク（`navigator.mediaDevices.getUserMedia()`）、位置情報（Geolocation API）、通知
- デバイスアクセスの権限とセキュリティ上の前提を理解している。
  - ユーザー許可の取得プロセスと、拒否された場合の挙動
  - セキュアコンテキスト（HTTPS）要件
  - プライバシーとセキュリティの配慮事項

### 8.3. 依存の取り込み (重要度: 2)
出題種別: 知識問題、記述問題

#### 概要
- 外部パッケージやスクリプトの取り込みがアプリケーションに及ぼすセキュリティ上の影響を理解している。

#### 詳細
- サードパーティ依存の信頼境界と、取り込み時に確認すべき観点を理解している。
- 既知の脆弱性や意図しない権限付与が及ぼす影響を理解している。
- 4.2 節のパッケージ管理と配布の流れとの関係を理解している。
